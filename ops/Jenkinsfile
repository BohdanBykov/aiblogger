pipeline {
	agent any

	stages {

		stage("start ansible-playbook") {
			
			steps {
				ansiblePlaybook(String /ops/deploy_on_docker.yaml) {
					becomeUser(String user = 'ubuntu')
					sudo(boolean become = 'true')
					hostKeyChecking(boolean hostKeyChecking = false)
				}
			}
		}
	}
}