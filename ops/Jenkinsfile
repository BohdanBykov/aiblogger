pipeline {
    stages {

        stage('run ansible playbook') {
            steps {
                ansiblePlaybook('ops/deploy_on_docker.yaml')
                    become(true)
                    becomeUser('ubuntu')
                    
            }           
        }
    }
}