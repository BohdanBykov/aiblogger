pipeline {
	agent any

	stages {

		stage("start ansible-playbook") {
			
			steps {
				ansiblePlaybook('ops/deploy_on_docker.yaml') {
					becomeUser('ubuntu')
					sudo('true')
					hostKeyChecking(false)
				}
			}
		}
	}
}