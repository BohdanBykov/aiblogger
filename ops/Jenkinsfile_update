pipeline {
    agent any 
    stages {
        stage('run playbook') { 
            steps {
            
                ansiblePlaybook(
                    playbook: 'ops/update_on_docker.yaml',
                    hostKeyChecking: false,
                    become: true
                    )
            
            }
        }
    }
}